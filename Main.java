import app.App;
public class Main {
    public static void main(String[] args) {
       // Password password = new Password(30);
       // System.out.println("Password: " + password.getPassword());
       new App().start();
    }
}